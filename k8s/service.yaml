apiVersion: v1
kind: Service
metadata:
  name: bank-api-service
  labels:
    app: bank-api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: bank-api
